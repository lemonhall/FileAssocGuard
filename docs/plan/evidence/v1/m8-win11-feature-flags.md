# Evidence — v1 M8 Win11 feature flags workaround

## Commands

- `cargo test`
- `cargo run -p fag-cli -- features status --id 43229420 --type runtime`
- `cargo run -p fag-cli -- win11 disable-userchoicelatest`

## Output (fill in on target machine)

TODO: paste command output here once verified with the desired Windows build / privilege level.

